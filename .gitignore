# Secrets — never commit
.env
.env.*
!.env.example
.secrets
*.pem
*.key
*.p12
*.pfx
secrets/
credentials/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.claude/

# Language ecosystems (forward-looking)
node_modules/
.pnp.*
dist/
build/
.next/
.turbo/
__pycache__/
*.py[cod]
.venv/
.python-version
venv/
.ruff_cache/
.mypy_cache/
.pytest_cache/

# Go
*.test
*.out
coverage.txt
# Compiled binaries — paths only, not bare names (otherwise we'd accidentally
# ignore the cmd/loamss/ source directory).
runtime/bin/
loamss-*.tar.gz
loamss-*.zip

# Runtime local state (when the daemon exists)
# Use specific paths, not bare names, so we don't accidentally
# match source directories (e.g., runtime/internal/audit/).
.loamss/
loamss-data/
*.sqlite
*.sqlite3
*.db

# Logs
*.log
logs/

# Local Claude Code state, etc.
.local/
